{
  "name": "{{.Name}}",
  "version": "1.0.0",
  "type": "api",{{if .Database}}
  "database": {
    "type": "{{.Database}}"
  },{{end}}
  "commands": {
    "init": "go mod tidy",
    "run": "go run ./main.go",
    "build": "go build -o bin/app ."
  }
}
