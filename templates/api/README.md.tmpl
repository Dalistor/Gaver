# {{.Name}}

API gerada com [Gaver](https://github.com/Dalistor/gaver).

## Configuração

Ajuste as variáveis no arquivo `.env`:

```sh
APP_PORT=8080
{{- if .Database}}
# Conexão com {{.Database}}
{{- end}}
```

## Comandos

```sh
gaver init   # instala dependências
gaver run    # executa o projeto
gaver build  # compila o projeto
```

## Estrutura

```
src/
├── engine/     → motor HTTP{{if .Database}} e banco de dados{{end}}
└── modules/    → módulos de domínio da regra de negócio
```
