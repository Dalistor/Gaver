package {{.ModuleName}}

import (
	"net/http"

	"{{.ProjectName}}/src/engine"
)

type Module struct{}

func New() *Module {
	return &Module{}
}

func (m *Module) Name() string {
	return "{{.ModuleName}}"
}

func (m *Module) Setup(e *engine.Engine) error {
	e.Handle("/{{.ModuleName}}", http.HandlerFunc(m.handle))
	return nil
}

func (m *Module) handle(w http.ResponseWriter, r *http.Request) {
	w.WriteHeader(http.StatusOK)
}
